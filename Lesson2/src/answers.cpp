#include <answers.h>

std::vector<int>& nSum(int n) {
    std::vector<int>* container = new std::vector<int>();

    // Your code here...

    return *container;
}

std::vector<int>& copyVec(std::vector<int>& toCopy) {
    std::vector<int>* newVec = new std::vector<int>();

    // Your code here...

    return *newVec;
}